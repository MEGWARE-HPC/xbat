name: Build and Test xbat daemon
#runner now running as root
on:
  push:
    branches:
      - ci-testing
  pull_request:
    branches:
      - ci-testing

jobs:
  test-el8:
    uses: ./.github/workflows/vm-test.yml
    with:
      distro: 8
      version: 1.0.0
      release: rc0